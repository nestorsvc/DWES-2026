<?php
function area($base, $altura)
{
    $area = ($base * $altura) / 2;
    // if (is_int($area)) {
    //     return $area;
    // } else if (is_float($area)) {
    //     return $area;
    // }
    return $area;
}
